apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ha-platform
  labels:
    app: postgres
spec:
  ports:
  - port: 5432
    name: postgres
  clusterIP: None  # HEADLESS SERVICE: Critical for StatefulSet identity
  selector:
    app: postgres